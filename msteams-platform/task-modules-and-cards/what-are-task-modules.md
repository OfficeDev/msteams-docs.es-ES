---
title: ¿Qué son los módulos de tareas?
author: clearab
description: Agregar experiencias emergentes modales para recopilar o Mostrar información a los usuarios de las aplicaciones de Microsoft Teams.
ms.topic: overview
ms.author: anclear
ms.openlocfilehash: 22fdc7a9dab1ff6f27e2b0d144e54676b6cca50e
ms.sourcegitcommit: fdcd91b270d4c2e98ab2b2c1029c76c49bb807fa
ms.translationtype: MT
ms.contentlocale: es-ES
ms.lasthandoff: 03/13/2020
ms.locfileid: "44801467"
---
# <a name="what-are-task-modules"></a><span data-ttu-id="ca5a8-103">¿Qué son los módulos de tareas?</span><span class="sxs-lookup"><span data-stu-id="ca5a8-103">What are task modules?</span></span>

<span data-ttu-id="ca5a8-104">Los módulos de tareas le permiten crear experiencias de elemento emergente modal en su aplicación de Teams.</span><span class="sxs-lookup"><span data-stu-id="ca5a8-104">Task modules allow you to create modal popup experiences in your Teams application.</span></span> <span data-ttu-id="ca5a8-105">Dentro del elemento emergente, puede ejecutar su propio código HTML/JavaScript personalizado, mostrar un `<iframe>` widget basado en un (por ejemplo, YouTube o Microsoft Stream video) o mostrar una [tarjeta adaptable](/adaptive-cards/).</span><span class="sxs-lookup"><span data-stu-id="ca5a8-105">Inside the popup you can run your own custom HTML/JavaScript code, show an `<iframe>`-based widget such as a YouTube or Microsoft Stream video or display an [Adaptive card](/adaptive-cards/).</span></span> <span data-ttu-id="ca5a8-106">Son especialmente útiles para iniciar y completar tareas o para mostrar información enriquecida como vídeos o paneles de Power BI.</span><span class="sxs-lookup"><span data-stu-id="ca5a8-106">They are especially useful for initiating and completing tasks or displaying rich information like videos or Power BI dashboards.</span></span> <span data-ttu-id="ca5a8-107">A menudo, una experiencia emergente es más natural para los usuarios que inician y completan tareas en comparación con una experiencia de ficha o de robot basada en conversaciones.</span><span class="sxs-lookup"><span data-stu-id="ca5a8-107">A popup experience is often more natural for users initiating and completing tasks compared to a tab or a conversation-based bot experience.</span></span>

<span data-ttu-id="ca5a8-108">Los módulos de tareas se basan en la base de las pestañas de Microsoft Teams; Básicamente, se trata de una pestaña dentro de una ventana emergente.</span><span class="sxs-lookup"><span data-stu-id="ca5a8-108">Task modules build on the foundation of Microsoft Teams tabs; they are essentially a tab inside a popup window.</span></span> <span data-ttu-id="ca5a8-109">Usan el mismo SDK, por lo que, si ha creado una pestaña, ya es 90% del camino para poder crear un módulo de tareas.</span><span class="sxs-lookup"><span data-stu-id="ca5a8-109">They use the same SDK, so if you've built a tab you are already 90% of the way to being able to create a task module.</span></span>

<span data-ttu-id="ca5a8-110">Los módulos de tareas se pueden invocar de tres maneras:</span><span class="sxs-lookup"><span data-stu-id="ca5a8-110">Task modules can be invoked in three ways:</span></span>

* <span data-ttu-id="ca5a8-111">**Fichas de canal o personal.**</span><span class="sxs-lookup"><span data-stu-id="ca5a8-111">**Channel or personal tabs.**</span></span> <span data-ttu-id="ca5a8-112">Mediante el SDK de pestañas de Microsoft Teams, puede invocar módulos de tareas desde botones, vínculos o menús en la ficha. [esto se describe en detalle aquí.](~/task-modules-and-cards/task-modules/task-modules-tabs.md)</span><span class="sxs-lookup"><span data-stu-id="ca5a8-112">Using the Microsoft Teams Tabs SDK you can invoke task modules from buttons, links or menus on your tab. [This is covered in detail here.](~/task-modules-and-cards/task-modules/task-modules-tabs.md)</span></span>
* <span data-ttu-id="ca5a8-113">**Transferido.**</span><span class="sxs-lookup"><span data-stu-id="ca5a8-113">**Bots.**</span></span> <span data-ttu-id="ca5a8-114">Botones en [tarjetas](~/task-modules-and-cards/cards/cards-reference.md) enviadas desde el bot.</span><span class="sxs-lookup"><span data-stu-id="ca5a8-114">Buttons on [cards](~/task-modules-and-cards/cards/cards-reference.md) sent from your bot.</span></span> <span data-ttu-id="ca5a8-115">Esto es especialmente útil cuando no es necesario que todos los usuarios estén en un canal para ver lo que está haciendo con un bot.</span><span class="sxs-lookup"><span data-stu-id="ca5a8-115">This is particularly useful when you don't need everyone in a channel to see what you are doing with a bot.</span></span> <span data-ttu-id="ca5a8-116">Por ejemplo, cuando los usuarios responden a un sondeo en un canal, no es muy útil ver un registro del sondeo que se está creando.</span><span class="sxs-lookup"><span data-stu-id="ca5a8-116">For example, when having users respond to a poll in a channel it's not terribly useful to see a record of that poll being created.</span></span> [<span data-ttu-id="ca5a8-117">Esto se describe en detalle aquí.</span><span class="sxs-lookup"><span data-stu-id="ca5a8-117">This is covered in detail here.</span></span>](~/task-modules-and-cards/task-modules/task-modules-bots.md)
* <span data-ttu-id="ca5a8-118">**Fuera de los equipos desde un vínculo profundo.**</span><span class="sxs-lookup"><span data-stu-id="ca5a8-118">**Outside of Teams from a deep link.**</span></span> <span data-ttu-id="ca5a8-119">También puede crear direcciones URL para invocar un módulo de tareas desde cualquier lugar.</span><span class="sxs-lookup"><span data-stu-id="ca5a8-119">You can also create URLs to invoke a task module from anywhere.</span></span> [<span data-ttu-id="ca5a8-120">Esto se describe en detalle aquí.</span><span class="sxs-lookup"><span data-stu-id="ca5a8-120">This is covered in detail here.</span></span>](#task-module-deep-link-syntax)

## <a name="what-a-task-module-looks-like"></a><span data-ttu-id="ca5a8-121">Aspecto de un módulo de tareas</span><span class="sxs-lookup"><span data-stu-id="ca5a8-121">What a task module looks like</span></span>

<span data-ttu-id="ca5a8-122">Así es como se ve un módulo de tareas cuando se invoca desde un bot (sin los rectángulos coloreados y círculos numerados, por supuesto):</span><span class="sxs-lookup"><span data-stu-id="ca5a8-122">Here's what a task module looks like when invoked from a bot (without the colored rectangles and numbered circles, of course):</span></span>

![Ejemplo de módulo de tarea](~/assets/images/task-module/task-module-example.png)

<span data-ttu-id="ca5a8-124">Vamos a examinarlo:</span><span class="sxs-lookup"><span data-stu-id="ca5a8-124">Let's walk through it:</span></span>

1. <span data-ttu-id="ca5a8-125">[ `color` Icono](~/resources/schema/manifest-schema.md#icons)de la aplicación.</span><span class="sxs-lookup"><span data-stu-id="ca5a8-125">Your app's [`color` icon](~/resources/schema/manifest-schema.md#icons).</span></span>
2. <span data-ttu-id="ca5a8-126">El [ `short` nombre](~/resources/schema/manifest-schema.md#name)de la aplicación.</span><span class="sxs-lookup"><span data-stu-id="ca5a8-126">Your app's [`short` name](~/resources/schema/manifest-schema.md#name).</span></span>
3. <span data-ttu-id="ca5a8-127">Título del módulo de tarea especificado en la `title` propiedad del [objeto TaskInfo](#the-taskinfo-object).</span><span class="sxs-lookup"><span data-stu-id="ca5a8-127">The task module's title specified in the `title` property of the [TaskInfo object](#the-taskinfo-object).</span></span>
4. <span data-ttu-id="ca5a8-128">El botón Cerrar/cancelar del módulo de tareas.</span><span class="sxs-lookup"><span data-stu-id="ca5a8-128">The task module's close/cancel button.</span></span> <span data-ttu-id="ca5a8-129">Si el usuario presiona esto, la aplicación recibirá un `err` evento tal y como se describe [aquí](~/task-modules-and-cards/task-modules/task-modules-tabs.md#example-submitting-the-result-of-a-task-module).</span><span class="sxs-lookup"><span data-stu-id="ca5a8-129">If the user presses this, your app will receive an `err` event as described [here](~/task-modules-and-cards/task-modules/task-modules-tabs.md#example-submitting-the-result-of-a-task-module).</span></span> <span data-ttu-id="ca5a8-130">(**Nota:** actualmente no es posible detectar este evento cuando se invoca un módulo de tareas desde un bot).</span><span class="sxs-lookup"><span data-stu-id="ca5a8-130">(**Note:** It is currently not possible to detect this event when a task module is invoked from a bot.)</span></span>
5. <span data-ttu-id="ca5a8-131">El rectángulo azul es donde aparece la página web si está cargando su propia página web con la `url` propiedad del [objeto TaskInfo](#the-taskinfo-object).</span><span class="sxs-lookup"><span data-stu-id="ca5a8-131">The blue rectangle is the where your web page appears if you are loading your own web page using the `url` property of the [TaskInfo object](#the-taskinfo-object).</span></span> <span data-ttu-id="ca5a8-132">Encontrará más detalles en la sección [tamaño del módulo de tareas](#task-module-sizing) que aparece a continuación.</span><span class="sxs-lookup"><span data-stu-id="ca5a8-132">More detail is in the [task module sizing](#task-module-sizing) section below.</span></span>
6. <span data-ttu-id="ca5a8-133">Si va a mostrar una tarjeta adaptable a través `card` de la propiedad del [objeto TaskInfo](#the-taskinfo-object) , el relleno se agrega de forma manual; de lo contrario, deberá [controlarlo por su cuenta](#task-module-css-for-htmljavascript-task-modules).</span><span class="sxs-lookup"><span data-stu-id="ca5a8-133">If you are displaying an Adaptive card via the `card` property of the [TaskInfo object](#the-taskinfo-object) the padding is added for you, otherwise you'll need to [handle this yourself](#task-module-css-for-htmljavascript-task-modules).</span></span>
7. <span data-ttu-id="ca5a8-134">Los botones de tarjeta adaptable se representarán aquí.</span><span class="sxs-lookup"><span data-stu-id="ca5a8-134">Adaptive card buttons will render here.</span></span> <span data-ttu-id="ca5a8-135">Si está usando su propia página, debe crear sus propios botones.</span><span class="sxs-lookup"><span data-stu-id="ca5a8-135">If you're using your own page you must create your own buttons.</span></span>

## <a name="overview-of-invoking-and-dismissing-task-modules"></a><span data-ttu-id="ca5a8-136">Información general sobre cómo invocar y desechar módulos de tareas</span><span class="sxs-lookup"><span data-stu-id="ca5a8-136">Overview of invoking and dismissing task modules</span></span>

<span data-ttu-id="ca5a8-137">Los módulos de tareas se pueden invocar desde fichas, bots o vínculos profundos, y lo que aparece en uno puede ser HTML o una tarjeta adaptable, por lo que hay mucha flexibilidad en cuanto a cómo se invocan y cómo tratar con el resultado de la interacción del usuario.</span><span class="sxs-lookup"><span data-stu-id="ca5a8-137">Task modules can be invoked from tabs, bots or deep links and what appears in one can be either HTML or an Adaptive card, so there's a lot of flexibility in terms of how they are invoked and how to deal with the result of a user's interaction.</span></span> <span data-ttu-id="ca5a8-138">La siguiente tabla resume cómo funciona esto:</span><span class="sxs-lookup"><span data-stu-id="ca5a8-138">The table below summarizes how this works:</span></span>

| <span data-ttu-id="ca5a8-139">**Invocado a través de...**</span><span class="sxs-lookup"><span data-stu-id="ca5a8-139">**Invoked via...**</span></span> | <span data-ttu-id="ca5a8-140">**El módulo de tareas es HTML/JavaScript**</span><span class="sxs-lookup"><span data-stu-id="ca5a8-140">**Task module is HTML/JavaScript**</span></span> | <span data-ttu-id="ca5a8-141">**El módulo de tareas es una tarjeta adaptable**</span><span class="sxs-lookup"><span data-stu-id="ca5a8-141">**Task module is Adaptive card**</span></span> |
| --- | --- | --- |
| <span data-ttu-id="ca5a8-142">**JavaScript en una pestaña**</span><span class="sxs-lookup"><span data-stu-id="ca5a8-142">**JavaScript in a tab**</span></span> | <span data-ttu-id="ca5a8-143">1. usar la función cliente de Teams `tasks.startTask()` con una `submitHandler(err, result)` función de devolución de llamada opcional</span><span class="sxs-lookup"><span data-stu-id="ca5a8-143">1. Use the Teams client SDK function `tasks.startTask()` with an optional `submitHandler(err, result)` callback function</span></span> <br/><br/> <span data-ttu-id="ca5a8-144">2. en el código del módulo de tareas, cuando el usuario haya finalizado, llame a la función de Team SDK `tasks.submitTask()` con un `result` objeto como parámetro.</span><span class="sxs-lookup"><span data-stu-id="ca5a8-144">2. In the task module code, when the user is finished, call the Teams SDK function `tasks.submitTask()` with a `result` object as a parameter.</span></span> <span data-ttu-id="ca5a8-145">Si `submitHandler` se especificó una devolución de llamada en `tasks.startTask()` , teams la llama `result` como un parámetro.</span><span class="sxs-lookup"><span data-stu-id="ca5a8-145">If a `submitHandler` callback was specified in `tasks.startTask()`, Teams calls it with `result` as a parameter.</span></span><br/><br/> <span data-ttu-id="ca5a8-146">3. Si se produce un error al invocar `tasks.startTask()` , `submitHandler` se llama a la función con una `err` cadena en su lugar.</span><span class="sxs-lookup"><span data-stu-id="ca5a8-146">3. If there was an error when invoking `tasks.startTask()`, the `submitHandler` function is called with an `err` string instead.</span></span> <br/><br/> <span data-ttu-id="ca5a8-147">4. también puede especificar un `completionBotId` cuando la llamada `teams.startTask()` , en cuyo caso `result` se envía al bot en su lugar.</span><span class="sxs-lookup"><span data-stu-id="ca5a8-147">4. You can also specify a `completionBotId` when calling `teams.startTask()` - in that case `result` is sent to the bot instead.</span></span> | <span data-ttu-id="ca5a8-148">1. llamar a la función del SDK del cliente de Microsoft Teams `tasks.startTask()` con un [objeto TaskInfo](#the-taskinfo-object) y `TaskInfo.card` que contenga el JSON de la tarjeta adaptable para mostrar en el menú emergente del módulo de tareas.</span><span class="sxs-lookup"><span data-stu-id="ca5a8-148">1. Call the Teams client SDK function `tasks.startTask()` with a [TaskInfo object](#the-taskinfo-object) and `TaskInfo.card` containing the JSON for the Adaptive card to show in the task module popup.</span></span> <br/><br/> <span data-ttu-id="ca5a8-149">2. Si `submitHandler` se especificó una devolución de llamada en `tasks.startTask()` , teams la llama con una `err` cadena si se produjo un error al invocar `tasks.startTask()` o si el usuario cierra el elemento emergente del módulo de tarea con la X en la esquina superior derecha.</span><span class="sxs-lookup"><span data-stu-id="ca5a8-149">2. If a `submitHandler` callback was specified in `tasks.startTask()`, Teams calls it with an `err` string if there was an error when invoking `tasks.startTask()` or if the user closes the task module popup using the X at the upper right.</span></span> <br/><br/> <span data-ttu-id="ca5a8-150">3. Si el usuario presiona un botón Action. Submit, su `data` objeto se devuelve como el valor de `result` .</span><span class="sxs-lookup"><span data-stu-id="ca5a8-150">3. If the user presses an Action.Submit button then its `data` object is returned as the value of `result`.</span></span> |
| <span data-ttu-id="ca5a8-151">**Botón ficha bot**</span><span class="sxs-lookup"><span data-stu-id="ca5a8-151">**Bot card button**</span></span> | <span data-ttu-id="ca5a8-152">1. los botones de la tarjeta de bot, según el tipo de botón, pueden invocar módulos de tareas de dos maneras: una dirección URL de vínculo profundo o mediante el envío de un `task/fetch` mensaje.</span><span class="sxs-lookup"><span data-stu-id="ca5a8-152">1. Bot card buttons, depending on the type of button, can invoke task modules in two ways: a deep link URL or by sending a `task/fetch` message.</span></span> <span data-ttu-id="ca5a8-153">Vea a continuación cómo funcionan las direcciones URL de vínculo en profundidad.</span><span class="sxs-lookup"><span data-stu-id="ca5a8-153">See below for how deep link URLs work.</span></span> <br/><br/> <span data-ttu-id="ca5a8-154">2. Si la acción del botón `type` es `task/fetch` ( `Action.Submit` tipo de botón para tarjetas adaptables), `task/fetch invoke` se envía un evento (un post http en las cubiertas) al bot y el bot responde a la entrada con http 200 y al cuerpo de la respuesta que contiene un contenedor alrededor del [objeto TaskInfo](#the-taskinfo-object).</span><span class="sxs-lookup"><span data-stu-id="ca5a8-154">2. If the button's action `type` is `task/fetch` (`Action.Submit` button type for Adaptive cards), a `task/fetch invoke` event (an HTTP POST under the covers) is sent to the bot, and the bot responds to the POST with HTTP 200 and the response body containing a wrapper around the [TaskInfo object](#the-taskinfo-object).</span></span> <span data-ttu-id="ca5a8-155">Esto se explica en detalle en [invocación de un módulo de tareas a través de la tarea/obtención](~/task-modules-and-cards/task-modules/task-modules-bots.md#invoking-a-task-module-via-taskfetch).</span><span class="sxs-lookup"><span data-stu-id="ca5a8-155">This is explained in detail in [invoking a task module via task/fetch](~/task-modules-and-cards/task-modules/task-modules-bots.md#invoking-a-task-module-via-taskfetch).</span></span><br/><br/> <span data-ttu-id="ca5a8-156">3. Teams muestra el módulo de tareas; Cuando el usuario haya finalizado, llame a la función de Team SDK `tasks.submitTask()` con un `result` objeto como parámetro.</span><span class="sxs-lookup"><span data-stu-id="ca5a8-156">3. Teams displays the task module; when the user is finished, call the Teams SDK function `tasks.submitTask()` with a `result` object as a parameter.</span></span> <br/><br/> <span data-ttu-id="ca5a8-157">4. el bot recibe un `task/submit invoke` mensaje que contiene el `result` objeto.</span><span class="sxs-lookup"><span data-stu-id="ca5a8-157">4. The bot receives a `task/submit invoke` message that contains the `result` object.</span></span> <span data-ttu-id="ca5a8-158">Tiene tres formas diferentes de responder al `task/submit` mensaje: no hacer nada (la tarea se completó correctamente), mostrar un mensaje al usuario en una ventana emergente o invocar otra ventana del módulo de tareas (es decir, la creación de una experiencia similar a un asistente).</span><span class="sxs-lookup"><span data-stu-id="ca5a8-158">You have three different ways to respond to the `task/submit` message: by doing nothing (the task completed successfully), by displaying a message to the user in a popup window, or by invoking another task module window (i.e. creating a wizard-like experience).</span></span> <span data-ttu-id="ca5a8-159">Estas tres opciones se describen más [en la descripción detallada sobre la tarea o el envío](~/task-modules-and-cards/task-modules/task-modules-bots.md#the-flexibility-of-tasksubmit).</span><span class="sxs-lookup"><span data-stu-id="ca5a8-159">These three options are discussed more [in the detailed discussion on task/submit](~/task-modules-and-cards/task-modules/task-modules-bots.md#the-flexibility-of-tasksubmit).</span></span> | <span data-ttu-id="ca5a8-160">1. al igual que los botones de las tarjetas de la plataforma de bots, los botones de las tarjetas adaptables admiten dos formas de invocar módulos de tareas: direcciones URL de vínculo profundas con `Action.openUrl` botones y mediante `task/fetch` `Action.Submit` botones.</span><span class="sxs-lookup"><span data-stu-id="ca5a8-160">1. Like buttons on Bot Framework cards, buttons on Adaptive cards support two ways of invoking task modules: deep link URLs with `Action.openUrl` buttons, and via `task/fetch` using `Action.Submit` buttons.</span></span> <br/><br/> <span data-ttu-id="ca5a8-161">2. los módulos de tareas con tarjetas adaptables funcionan de forma muy similar al caso de HTML/JavaScript (vea la izquierda).</span><span class="sxs-lookup"><span data-stu-id="ca5a8-161">2. Task modules with Adaptive cards work very similarly to the HTML/JavaScript case (see left).</span></span> <span data-ttu-id="ca5a8-162">La principal diferencia es que, dado que no hay JavaScript cuando se usan tarjetas adaptables, no hay forma de llamar `tasks.submitTask()` .</span><span class="sxs-lookup"><span data-stu-id="ca5a8-162">The major difference is that since there's no JavaScript when you're using Adaptive cards, there's no way to call `tasks.submitTask()`.</span></span> <span data-ttu-id="ca5a8-163">En su lugar, Teams toma el `data` objeto de `Action.Submit` y lo devuelve como la carga del `task/submit` evento, tal como se describe [aquí](~/task-modules-and-cards/task-modules/task-modules-bots.md#the-flexibility-of-tasksubmit).</span><span class="sxs-lookup"><span data-stu-id="ca5a8-163">Instead, Teams takes the `data` object from `Action.Submit` and returns it as the payload of in the `task/submit` event, as described [here](~/task-modules-and-cards/task-modules/task-modules-bots.md#the-flexibility-of-tasksubmit).</span></span> |
| <span data-ttu-id="ca5a8-164">**Dirección URL de vínculo profundo**</span><span class="sxs-lookup"><span data-stu-id="ca5a8-164">**Deep link URL**</span></span> <br/>[<span data-ttu-id="ca5a8-165">sintaxis URL</span><span class="sxs-lookup"><span data-stu-id="ca5a8-165">URL syntax</span></span>](#task-module-deep-link-syntax) | <span data-ttu-id="ca5a8-166">1. Microsoft Teams invoca el módulo de tareas; Dirección URL que aparece dentro del `<iframe>` especificado en el `url` parámetro del vínculo en profundidad.</span><span class="sxs-lookup"><span data-stu-id="ca5a8-166">1. Teams invokes the task module; the URL that appears inside the `<iframe>` specified in the `url` parameter of the deep link.</span></span> <span data-ttu-id="ca5a8-167">No hay ninguna `submitHandler` devolución de llamada.</span><span class="sxs-lookup"><span data-stu-id="ca5a8-167">There is no `submitHandler` callback.</span></span> <br/><br/> <span data-ttu-id="ca5a8-168">2. en el JavaScript de la página en el módulo de tareas, llame `tasks.submitTask()` a para cerrarla con un `result` objeto como parámetro, igual que cuando se invoca desde un botón de ficha o de tarjeta de bot.</span><span class="sxs-lookup"><span data-stu-id="ca5a8-168">2. Within the JavaScript of the page in the task module, call `tasks.submitTask()` to close it with a `result` object as a parameter, the same as when invoking it from a tab or a bot card button.</span></span> <span data-ttu-id="ca5a8-169">Sin embargo, la lógica de finalización es ligeramente diferente.</span><span class="sxs-lookup"><span data-stu-id="ca5a8-169">Completion logic is slightly different, however.</span></span> <span data-ttu-id="ca5a8-170">Si la lógica de finalización reside en el cliente (es decir, si no hay ningún bot), no hay ninguna `submitHandler` devolución de llamada, por lo que cualquier lógica de finalización debe estar en el código anterior a la llamada a `tasks.submitTask()` .</span><span class="sxs-lookup"><span data-stu-id="ca5a8-170">If your completion logic resides on the client (i.e. if there is no bot) there is no `submitHandler` callback, so any completion logic must be in the code preceding the call to `tasks.submitTask()`.</span></span> <span data-ttu-id="ca5a8-171">Los errores de invocación solo se notifican a través de la consola.</span><span class="sxs-lookup"><span data-stu-id="ca5a8-171">Invocation errors are only reported via the console.</span></span> <span data-ttu-id="ca5a8-172">Si tiene un bot, puede especificar un `completionBotId` parámetro en el vínculo profundo para enviar el `result` objeto a través de un `task/submit` evento.</span><span class="sxs-lookup"><span data-stu-id="ca5a8-172">If you have a bot, then you can specify a `completionBotId` parameter in the deep link to send the `result` object via a `task/submit` event.</span></span> | <span data-ttu-id="ca5a8-173">1. Microsoft Teams invoca el módulo de tareas; el cuerpo de la tarjeta JSON de la tarjeta adaptable se especifica como un valor codificado como URL del `card` parámetro del vínculo profundo.</span><span class="sxs-lookup"><span data-stu-id="ca5a8-173">1. Teams invokes the task module; the JSON card body of the Adaptive card is specified as a URL-encoded value of the `card` parameter of the deep link.</span></span> <br/><br/> <span data-ttu-id="ca5a8-174">2. el usuario cierra el módulo de tareas haciendo clic en la X en la esquina superior derecha del módulo de tareas o presionando un `Action.Submit` botón de la tarjeta.</span><span class="sxs-lookup"><span data-stu-id="ca5a8-174">2. The user closes the task module by clicking on the X at the upper right of the task module or by pressing an `Action.Submit` button on the card.</span></span> <span data-ttu-id="ca5a8-175">Como no hay `submitHandler` llamada a, debe tener un bot para enviar el valor de los campos de la tarjeta adaptable a.</span><span class="sxs-lookup"><span data-stu-id="ca5a8-175">Since there is no `submitHandler` to call, you must have a bot to send the value of the Adaptive card fields to.</span></span> <span data-ttu-id="ca5a8-176">Use el `completionBotId` parámetro en el vínculo profundo para especificar el bot al que se van a enviar los datos a través de un `task/submit invoke` evento.</span><span class="sxs-lookup"><span data-stu-id="ca5a8-176">You use the `completionBotId` parameter in the deep link to specify the bot to send the data to via a `task/submit invoke` event.</span></span> |

> [!NOTE]
> <span data-ttu-id="ca5a8-177">La invocación de un módulo de tareas desde JavaScript no se admite en dispositivos móviles.</span><span class="sxs-lookup"><span data-stu-id="ca5a8-177">Invoking a task module from JavaScript is not supported on mobile.</span></span>

## <a name="the-taskinfo-object"></a><span data-ttu-id="ca5a8-178">El objeto TaskInfo</span><span class="sxs-lookup"><span data-stu-id="ca5a8-178">The TaskInfo object</span></span>

<span data-ttu-id="ca5a8-179">El `TaskInfo` objeto contiene los metadatos de un módulo de tareas.</span><span class="sxs-lookup"><span data-stu-id="ca5a8-179">The `TaskInfo` object contains the metadata for a task module.</span></span> <span data-ttu-id="ca5a8-180">A continuación se muestra la definición del objeto.</span><span class="sxs-lookup"><span data-stu-id="ca5a8-180">The object definition is below.</span></span> <span data-ttu-id="ca5a8-181">**Debe** definir `url` (for en iframe incrustado) o `card` (para una tarjeta adaptable).</span><span class="sxs-lookup"><span data-stu-id="ca5a8-181">You **must** define either `url` (for en embedded iFrame) or `card` (for an Adaptive Card).</span></span>

| <span data-ttu-id="ca5a8-182">Atributo</span><span class="sxs-lookup"><span data-stu-id="ca5a8-182">Attribute</span></span> | <span data-ttu-id="ca5a8-183">Tipo</span><span class="sxs-lookup"><span data-stu-id="ca5a8-183">Type</span></span> | <span data-ttu-id="ca5a8-184">Descripción</span><span class="sxs-lookup"><span data-stu-id="ca5a8-184">Description</span></span> |
| --- | --- | --- |
| `title` | <span data-ttu-id="ca5a8-185">string</span><span class="sxs-lookup"><span data-stu-id="ca5a8-185">string</span></span> | <span data-ttu-id="ca5a8-186">Aparece debajo del nombre de la aplicación y a la derecha del icono de la aplicación</span><span class="sxs-lookup"><span data-stu-id="ca5a8-186">Appears below the app name and to the right of the app icon</span></span> |
| `height` | <span data-ttu-id="ca5a8-187">número o cadena</span><span class="sxs-lookup"><span data-stu-id="ca5a8-187">number or string</span></span> | <span data-ttu-id="ca5a8-188">Puede ser un número que representa el alto del módulo de tareas en píxeles, o `small` , `medium` o `large` .</span><span class="sxs-lookup"><span data-stu-id="ca5a8-188">This can be a number representing the task module's height in pixels, or `small`, `medium`, or `large`.</span></span> <span data-ttu-id="ca5a8-189">[Vea a continuación cómo se administran el alto y el ancho](#task-module-sizing).</span><span class="sxs-lookup"><span data-stu-id="ca5a8-189">[See below for how height and width are handled](#task-module-sizing).</span></span> |
| `width` | <span data-ttu-id="ca5a8-190">número o cadena</span><span class="sxs-lookup"><span data-stu-id="ca5a8-190">number or string</span></span> | <span data-ttu-id="ca5a8-191">Puede ser un número que representa el ancho del módulo de tareas en píxeles, o `small` , `medium` o `large` .</span><span class="sxs-lookup"><span data-stu-id="ca5a8-191">This can be a number representing the task module's width in pixels, or `small`, `medium`, or `large`.</span></span> <span data-ttu-id="ca5a8-192">[Vea a continuación cómo se administran el alto y el ancho](#task-module-sizing).</span><span class="sxs-lookup"><span data-stu-id="ca5a8-192">[See below for how height and width are handled](#task-module-sizing).</span></span> |
| `url` | <span data-ttu-id="ca5a8-193">string</span><span class="sxs-lookup"><span data-stu-id="ca5a8-193">string</span></span> | <span data-ttu-id="ca5a8-194">La dirección URL de la página cargada como `<iframe>` dentro del módulo de tareas.</span><span class="sxs-lookup"><span data-stu-id="ca5a8-194">The URL of the page loaded as an `<iframe>` inside the task module.</span></span> <span data-ttu-id="ca5a8-195">El dominio de la dirección URL debe estar en la [matriz validDomains](~/resources/schema/manifest-schema.md#validdomains) de la aplicación en el manifiesto de la aplicación.</span><span class="sxs-lookup"><span data-stu-id="ca5a8-195">The URL's domain must be in the app's [validDomains array](~/resources/schema/manifest-schema.md#validdomains) in your app's manifest.</span></span> |
| `card` | <span data-ttu-id="ca5a8-196">Datos adjuntos de tarjeta de robot adaptable o tarjeta de adaptación</span><span class="sxs-lookup"><span data-stu-id="ca5a8-196">Adaptive card or an Adaptive card bot card attachment</span></span> | <span data-ttu-id="ca5a8-197">El JSON para que la tarjeta adaptable aparezca en el módulo de tareas.</span><span class="sxs-lookup"><span data-stu-id="ca5a8-197">The JSON for the Adaptive card to appear in the task module.</span></span> <span data-ttu-id="ca5a8-198">Si va a invocar desde un bot, deberá usar el JSON de la tarjeta adaptable en un objeto de bot Framework `attachment` .</span><span class="sxs-lookup"><span data-stu-id="ca5a8-198">If you're invoking from a bot, you'll need to use the Adaptive card JSON in a Bot Framework `attachment` object.</span></span> <span data-ttu-id="ca5a8-199">En una pestaña, usará solo una tarjeta adaptable.</span><span class="sxs-lookup"><span data-stu-id="ca5a8-199">From a tab you'll use just an Adaptive Card.</span></span> [<span data-ttu-id="ca5a8-200">Este es un ejemplo.</span><span class="sxs-lookup"><span data-stu-id="ca5a8-200">Here's an example.</span></span>](#adaptive-card-or-adaptive-card-bot-card-attachment) |
| `fallbackUrl` | <span data-ttu-id="ca5a8-201">string</span><span class="sxs-lookup"><span data-stu-id="ca5a8-201">string</span></span> | <span data-ttu-id="ca5a8-202">Si un cliente no admite la característica de módulo de tareas, esta dirección URL se abre en una pestaña del explorador.</span><span class="sxs-lookup"><span data-stu-id="ca5a8-202">If a client does not support the task module feature, this URL is opened in a browser tab.</span></span> |
| `completionBotId` | <span data-ttu-id="ca5a8-203">string</span><span class="sxs-lookup"><span data-stu-id="ca5a8-203">string</span></span> | <span data-ttu-id="ca5a8-204">Especifica un identificador de aplicación de bot para enviar el resultado de la interacción del usuario con el módulo de tareas a.</span><span class="sxs-lookup"><span data-stu-id="ca5a8-204">Specifies a bot App ID to send the result of the user's interaction with the task module to.</span></span> <span data-ttu-id="ca5a8-205">Si se especifica, el bot recibirá un `task/submit invoke` evento con un objeto JSON en la carga del evento.</span><span class="sxs-lookup"><span data-stu-id="ca5a8-205">If specified, the bot will receive a `task/submit invoke` event with a JSON object in the event payload.</span></span> |

> [!NOTE]
> <span data-ttu-id="ca5a8-206">La característica de módulo de tareas requiere que los dominios de las direcciones URL que desee cargar se incluyan en la matriz del manifiesto de la `validDomains` aplicación.</span><span class="sxs-lookup"><span data-stu-id="ca5a8-206">The task module feature requires that the domains of any URLs you want to load are included in the `validDomains` array in your app's manifest.</span></span>

## <a name="task-module-sizing"></a><span data-ttu-id="ca5a8-207">Tamaño del módulo de tareas</span><span class="sxs-lookup"><span data-stu-id="ca5a8-207">Task module sizing</span></span>

<span data-ttu-id="ca5a8-208">Si se usan enteros para `TaskInfo.width` y `TaskInfo.height` se establecerá el alto y el ancho en píxeles.</span><span class="sxs-lookup"><span data-stu-id="ca5a8-208">Using integers for `TaskInfo.width` and `TaskInfo.height` will set the height and width in pixels.</span></span> <span data-ttu-id="ca5a8-209">Sin embargo, en función del tamaño de la ventana y la resolución de la pantalla del equipo, se reducirán proporcionalmente, a la vez que se mantiene la relación de aspecto (ancho/alto).</span><span class="sxs-lookup"><span data-stu-id="ca5a8-209">However, depending on the size of the Team's window and screen resolution they will be reduced proportionally while maintaining the aspect ratio (width/height).</span></span>

<span data-ttu-id="ca5a8-210">Si `TaskInfo.width` y `TaskInfo.height` son `"small"` , `"medium"` o `"large"` el tamaño del rectángulo rojo de la imagen anterior es una proporción del espacio disponible: 20%, 50%, 60% para `width` y 20%, 50%, 66% para `height` .</span><span class="sxs-lookup"><span data-stu-id="ca5a8-210">If `TaskInfo.width` and `TaskInfo.height` are `"small"`, `"medium"` or `"large"` the size of the red rectangle in the picture above is a proportion of the available space: 20%, 50%, 60% for `width` and 20%, 50%, 66% for `height`.</span></span>

<span data-ttu-id="ca5a8-211">Los módulos de tareas invocados desde una ficha pueden cambiarse de tamaño dinámicamente.</span><span class="sxs-lookup"><span data-stu-id="ca5a8-211">Task modules invoked from a tab can be dynamically resized.</span></span> <span data-ttu-id="ca5a8-212">Después `tasks.startTask()` de llamar, puede llamar `tasks.updateTask(newSize)` a donde las propiedades Height y width del objeto NewSize se ajustan a la especificación TaskInfo (p. ej.</span><span class="sxs-lookup"><span data-stu-id="ca5a8-212">After calling `tasks.startTask()` you can call `tasks.updateTask(newSize)` where height and width properties on the newSize object conform to the TaskInfo spec (ex.</span></span> <span data-ttu-id="ca5a8-213">`{ height: 'medium', width: 'medium' }`).</span><span class="sxs-lookup"><span data-stu-id="ca5a8-213">`{ height: 'medium', width: 'medium' }`).</span></span>

## <a name="task-module-css-for-htmljavascript-task-modules"></a><span data-ttu-id="ca5a8-214">CSS del módulo de tareas para módulos de tareas de HTML/JavaScript</span><span class="sxs-lookup"><span data-stu-id="ca5a8-214">Task module CSS for HTML/JavaScript task modules</span></span>

<span data-ttu-id="ca5a8-215">Los módulos de tareas basados en HTML/JavaScript tienen acceso a todo el área del módulo de tareas debajo del encabezado.</span><span class="sxs-lookup"><span data-stu-id="ca5a8-215">HTML/JavaScript-based task modules have access to the entire area of the task module below the header.</span></span> <span data-ttu-id="ca5a8-216">Aunque esto ofrece una gran flexibilidad, si desea que los bordes alrededor de los bordes se alineen con los elementos de encabezado y evite las barras de desplazamiento innecesarias, deberá proporcionar la CSS correcta.</span><span class="sxs-lookup"><span data-stu-id="ca5a8-216">While that offers a great deal of flexibility, if you want padding around the edges to align with the header elements and avoid unnecessary scrollbars you'll need to provide the right CSS.</span></span> <span data-ttu-id="ca5a8-217">A continuación se muestran algunos ejemplos de algunos casos de uso.</span><span class="sxs-lookup"><span data-stu-id="ca5a8-217">Here are some examples for a few use cases.</span></span>

### <a name="example-1---youtube-video"></a><span data-ttu-id="ca5a8-218">Ejemplo 1: vídeo de YouTube</span><span class="sxs-lookup"><span data-stu-id="ca5a8-218">Example 1 - YouTube video</span></span>

<span data-ttu-id="ca5a8-219">YouTube ofrece la capacidad de insertar vídeos en páginas Web.</span><span class="sxs-lookup"><span data-stu-id="ca5a8-219">YouTube offers the ability to embed videos on web pages.</span></span> <span data-ttu-id="ca5a8-220">Con una página web de código auxiliar sencilla es fácil mostrar esto en un módulo de tareas:</span><span class="sxs-lookup"><span data-stu-id="ca5a8-220">Using a simple stub web page it's easy to show this in a task module:</span></span>

![Vídeo de YouTube](~/assets/images/task-module/youtube-example.png)

<span data-ttu-id="ca5a8-222">Este es el HTML de esta página, sin CSS:</span><span class="sxs-lookup"><span data-stu-id="ca5a8-222">Here's the HTML for this page, without the CSS:</span></span>

```html
<!DOCTYPE html>
<html lang="en">
<head>
  ⋮
</head>
<body>
  <div id="embed-container">
    <iframe width="1000" height="700" src="https://www.youtube.com/embed/rd0Rd8w3FZ0" frameborder="0" allow="autoplay; encrypted-media" allowfullscreen=""></iframe>
  </div>
</body>
</html>
```

<span data-ttu-id="ca5a8-223">La CSS tiene el siguiente aspecto:</span><span class="sxs-lookup"><span data-stu-id="ca5a8-223">The CSS looks like this:</span></span>

```css
#embed-container iframe {
    position: absolute;
    top: 0;
    left: 0;
    width: 95%;
    height: 95%;
    padding-left: 20px;
    padding-right: 20px;
    padding-top: 10px;
    padding-bottom: 10px;
    border-style: none;
}
```

### <a name="example-2---powerapp"></a><span data-ttu-id="ca5a8-224">Ejemplo 2: PowerApp</span><span class="sxs-lookup"><span data-stu-id="ca5a8-224">Example 2 - PowerApp</span></span>

<span data-ttu-id="ca5a8-225">Puede usar el mismo enfoque para insertar una PowerApp también.</span><span class="sxs-lookup"><span data-stu-id="ca5a8-225">You can use the same approach to embed a PowerApp as well.</span></span> <span data-ttu-id="ca5a8-226">A medida que el alto o el ancho de una PowerApp individual es personalizable, es posible que deba ajustar la altura y el ancho para obtener la presentación deseada.</span><span class="sxs-lookup"><span data-stu-id="ca5a8-226">As the height/width of any individual PowerApp is customizable, you may need to adjust the height and width to achieve your desired presentation.</span></span>

![PowerApp de administración de activos](~/assets/images/task-module/powerapp-example.png)

```html
<iframe width="720" height="520" src="https://web.powerapps.com/webplayer/iframeapp?source=iframe&screenColor=rgba(104,101,171,1)&appId=/providers/Microsoft.PowerApps/apps/xxxxxxxx-xxxx-xxxx-xxxx-xxxxxxxxxxxx"></iframe>
```

<span data-ttu-id="ca5a8-228">Y el CSS es:</span><span class="sxs-lookup"><span data-stu-id="ca5a8-228">And the CSS is:</span></span>

```css
#embed-container iframe {
    position: absolute;
    top: 0;
    left: 0;
    width: 94%;
    height: 95%;
    padding-left: 20px;
    padding-right: 20px;
    padding-top: 10px;
    padding-bottom: 10px;
    border-style: none;
}
```

## <a name="adaptive-card-or-adaptive-card-bot-card-attachment"></a><span data-ttu-id="ca5a8-229">Archivo adjunto de tarjeta adaptable o tarjeta de robot adaptable</span><span class="sxs-lookup"><span data-stu-id="ca5a8-229">Adaptive card or Adaptive card bot card attachment</span></span>

<span data-ttu-id="ca5a8-230">Como se indicó anteriormente, en función de cómo invoque su, tendrá `card` que usar una tarjeta adaptable o un adjunto de tarjeta de bot? o de tarjeta adaptable (que es solo una tarjeta adaptable incluida en un objeto Attachment).</span><span class="sxs-lookup"><span data-stu-id="ca5a8-230">As we noted above, depending on how you're invoking your `card` you'll need to use either an Adaptive card, or an Adaptive card bot card attachment (which is just an Adaptive card wrapped in an attachment object).</span></span>

<span data-ttu-id="ca5a8-231">Cuando se realiza una llamada desde una pestaña, deberá usar una tarjeta adaptable.</span><span class="sxs-lookup"><span data-stu-id="ca5a8-231">When you're invoking from a tab, you'll need to use an adaptive card.</span></span> <span data-ttu-id="ca5a8-232">Este es un ejemplo muy sencillo:</span><span class="sxs-lookup"><span data-stu-id="ca5a8-232">Here's a very simple example:</span></span>

```json
{
    "type": "AdaptiveCard",
    "body": [
        {
            "type": "TextBlock",
            "text": "Here is a ninja cat:"
        },
        {
            "type": "Image",
            "url": "http://adaptivecards.io/content/cats/1.png",
            "size": "Medium"
        }
    ],
    "version": "1.0"
}
```

<span data-ttu-id="ca5a8-233">Al invocar desde un bot, deberá usar un adjunto de tarjeta de bot de tarjeta adaptable como en el ejemplo siguiente:</span><span class="sxs-lookup"><span data-stu-id="ca5a8-233">When you're invoking from a bot you'll need to use an Adaptive card bot card attachment as in the example below:</span></span>

```json
{
    "contentType": "application/vnd.microsoft.card.adaptive",
    "content": {
        "type": "AdaptiveCard",
        "body": [
            {
                "type": "TextBlock",
                "text": "Here is a ninja cat:"
            },
            {
                "type": "Image",
                "url": "http://adaptivecards.io/content/cats/1.png",
                "size": "Medium"
            }
        ],
        "version": "1.0"
    }
}
```

<span data-ttu-id="ca5a8-234">Deberá recordar si va a invocar un módulo de tareas que contiene una tarjeta adaptable desde un bot o una pestaña.</span><span class="sxs-lookup"><span data-stu-id="ca5a8-234">You'll need to remember whether you are invoking a task module containing an Adaptive card from a bot or a tab.</span></span>

## <a name="task-module-deep-link-syntax"></a><span data-ttu-id="ca5a8-235">Sintaxis de vínculo profundo del módulo de tareas</span><span class="sxs-lookup"><span data-stu-id="ca5a8-235">Task module deep link syntax</span></span>

<span data-ttu-id="ca5a8-236">Un vínculo profundo del módulo de tareas es solo una serialización del [objeto TaskInfo](#the-taskinfo-object) con dos otras partes de la información `APP_ID` y, de manera opcional, la `BOT_APP_ID` :</span><span class="sxs-lookup"><span data-stu-id="ca5a8-236">A task module deep link is just a serialization of the [TaskInfo object](#the-taskinfo-object) with two other pieces of information, `APP_ID` and optionally the `BOT_APP_ID`:</span></span>

`https://teams.microsoft.com/l/task/APP_ID?url=<TaskInfo.url>&height=<TaskInfo.height>&width=<TaskInfo.width>&title=<TaskInfo.title>&completionBotId=BOT_APP_ID`

`https://teams.microsoft.com/l/task/APP_ID?card=<TaskInfo.card>&height=<TaskInfo.height>&width=<TaskInfo.width>&title=<TaskInfo.title>&completionBotId=BOT_APP_ID`

<span data-ttu-id="ca5a8-237">Vea el [objeto TaskInfo](#the-taskinfo-object) para los tipos de datos y los valores permitidos para `<TaskInfo.url>` , `<TaskInfo.card>` , `<TaskInfo.height>` , `<TaskInfo.width>` y `<TaskInfo.title>` .</span><span class="sxs-lookup"><span data-stu-id="ca5a8-237">See [TaskInfo object](#the-taskinfo-object) for the data types and allowable values for `<TaskInfo.url>`, `<TaskInfo.card>`, `<TaskInfo.height>`, `<TaskInfo.width>`, and `<TaskInfo.title>`.</span></span>

> [!TIP]
> <span data-ttu-id="ca5a8-238">Asegúrese de codificar mediante URL el vínculo profundo, especialmente cuando use el `card` parámetro (por ejemplo, la [ `encodeURI()` función](https://www.w3schools.com/jsref/jsref_encodeURI.asp)de JavaScript).</span><span class="sxs-lookup"><span data-stu-id="ca5a8-238">Be sure to URL encode the deep link, especially when using the `card` parameter (for example, JavaScript's [`encodeURI()` function](https://www.w3schools.com/jsref/jsref_encodeURI.asp)).</span></span>

<span data-ttu-id="ca5a8-239">Esta es la información sobre `APP_ID` y `BOT_APP_ID` :</span><span class="sxs-lookup"><span data-stu-id="ca5a8-239">Here's the information on `APP_ID` and `BOT_APP_ID`:</span></span>

| <span data-ttu-id="ca5a8-240">Valor</span><span class="sxs-lookup"><span data-stu-id="ca5a8-240">Value</span></span> | <span data-ttu-id="ca5a8-241">Tipo</span><span class="sxs-lookup"><span data-stu-id="ca5a8-241">Type</span></span> | <span data-ttu-id="ca5a8-242">¿Necesario?</span><span class="sxs-lookup"><span data-stu-id="ca5a8-242">Required?</span></span> | <span data-ttu-id="ca5a8-243">Descripción</span><span class="sxs-lookup"><span data-stu-id="ca5a8-243">Description</span></span> |
| --- | --- | --- | --- |
| `APP_ID` | <span data-ttu-id="ca5a8-244">string</span><span class="sxs-lookup"><span data-stu-id="ca5a8-244">string</span></span> | <span data-ttu-id="ca5a8-245">Sí</span><span class="sxs-lookup"><span data-stu-id="ca5a8-245">Yes</span></span> | <span data-ttu-id="ca5a8-246">El [identificador](~/resources/schema/manifest-schema.md#id) de la aplicación que invoca el módulo de tareas.</span><span class="sxs-lookup"><span data-stu-id="ca5a8-246">The [id](~/resources/schema/manifest-schema.md#id) of the app invoking the task module.</span></span> <span data-ttu-id="ca5a8-247">La [matriz validDomains](~/resources/schema/manifest-schema.md#validdomains) del manifiesto `APP_ID` debe contener el dominio para `url` si `url` se encuentra en la dirección URL.</span><span class="sxs-lookup"><span data-stu-id="ca5a8-247">The [validDomains array](~/resources/schema/manifest-schema.md#validdomains) in the manifest for `APP_ID` must contain the domain for `url` if `url` is in the URL.</span></span> <span data-ttu-id="ca5a8-248">(El identificador de la aplicación ya se conoce cuando se invoca un módulo de tareas desde una pestaña o un bot, que es el motivo por el que no se incluye en `TaskInfo` ).</span><span class="sxs-lookup"><span data-stu-id="ca5a8-248">(The app ID is already known when a task module is invoked from a tab or a bot, which is why it's not included in `TaskInfo`.)</span></span> |
| `BOT_APP_ID` | <span data-ttu-id="ca5a8-249">string</span><span class="sxs-lookup"><span data-stu-id="ca5a8-249">string</span></span> | <span data-ttu-id="ca5a8-250">No</span><span class="sxs-lookup"><span data-stu-id="ca5a8-250">No</span></span> | <span data-ttu-id="ca5a8-251">Si se especifica un valor para `completionBotId` , el `result` objeto se envía a través de un `task/submit invoke` mensaje al bot especificado.</span><span class="sxs-lookup"><span data-stu-id="ca5a8-251">If a value for `completionBotId` is specified, the `result` object is sent via a a `task/submit invoke` message to the specified bot.</span></span> <span data-ttu-id="ca5a8-252">`BOT_APP_ID`debe especificarse como un bot en el manifiesto de la aplicación, es decir, no puede simplemente enviarlo a ningún bot.</span><span class="sxs-lookup"><span data-stu-id="ca5a8-252">`BOT_APP_ID` must be specified as a bot in the app's manifest, i.e. you can't just send it to any bot.</span></span> |

<span data-ttu-id="ca5a8-253">Tenga en cuenta que es válido `APP_ID` tanto para como `BOT_APP_ID` para ser el mismo y, en muchos casos, si una aplicación tiene un bot, ya que se recomienda usarlo como el identificador de una aplicación, en caso de existir.</span><span class="sxs-lookup"><span data-stu-id="ca5a8-253">Note that it's valid for `APP_ID` and `BOT_APP_ID` to be the same, and in many cases will be if an app has a bot since it's recommended to use that as an app's ID if there is one.</span></span>

## <a name="keyboard-and-accessibility-guidelines"></a><span data-ttu-id="ca5a8-254">Directrices de teclado y accesibilidad</span><span class="sxs-lookup"><span data-stu-id="ca5a8-254">Keyboard and accessibility guidelines</span></span>

<span data-ttu-id="ca5a8-255">Con los módulos de tareas basados en HTML/JavaScript, es su responsabilidad asegurarse de que el módulo de tareas de la aplicación puede usarse con un teclado.</span><span class="sxs-lookup"><span data-stu-id="ca5a8-255">With HTML/JavaScript-based task modules it is your responsibility to ensure your app's task module can be used with a keyboard.</span></span> <span data-ttu-id="ca5a8-256">Los programas de lector de pantalla también dependen de la capacidad de navegar con el teclado.</span><span class="sxs-lookup"><span data-stu-id="ca5a8-256">Screen reader programs also depend on the ability to navigate using the keyboard.</span></span> <span data-ttu-id="ca5a8-257">Como un asunto práctico, significa dos cosas:</span><span class="sxs-lookup"><span data-stu-id="ca5a8-257">As a practical matter this means two things:</span></span>

1. <span data-ttu-id="ca5a8-258">Usar el [atributo TabIndex](https://developer.mozilla.org/en-US/docs/Web/HTML/Global_attributes/tabindex) en las etiquetas HTML para controlar qué elementos se pueden centrar y si está participando en la navegación secuencial de teclado (normalmente con las teclas <kbd>Tab</kbd> y <kbd>MAYÚS-TAB</kbd> ).</span><span class="sxs-lookup"><span data-stu-id="ca5a8-258">Using the [tabindex attribute](https://developer.mozilla.org/en-US/docs/Web/HTML/Global_attributes/tabindex) in your HTML tags to control which elements can be focused and if/where it participates in sequential keyboard navigation (usually with the <kbd>Tab</kbd> and <kbd>Shift-Tab</kbd> keys).</span></span>
2. <span data-ttu-id="ca5a8-259">Control de la tecla <kbd>ESC</kbd> en JavaScript para el módulo de tareas.</span><span class="sxs-lookup"><span data-stu-id="ca5a8-259">Handling the <kbd>Esc</kbd> key in the JavaScript for your task module.</span></span> <span data-ttu-id="ca5a8-260">Este es un ejemplo de código que muestra cómo hacerlo:</span><span class="sxs-lookup"><span data-stu-id="ca5a8-260">Here's a code sample showing how to do this:</span></span>

  ```javascript
  // Handle the Esc key
  document.onkeyup = function(event) {
  if ((event.key === 27) || (event.key === "Escape")) {
    microsoftTeams.submitTask(null); // this will return an err object to the completionHandler()
    }
  }
  ```

<span data-ttu-id="ca5a8-261">Microsoft Teams garantizará que la navegación mediante teclado funciona correctamente desde el encabezado del módulo de tareas hasta el código HTML y viceversa.</span><span class="sxs-lookup"><span data-stu-id="ca5a8-261">Microsoft Teams will ensure that keyboard navigation works properly from the task module header into your HTML and vice-versa.</span></span>

## <a name="task-module-samples"></a><span data-ttu-id="ca5a8-262">Ejemplos de módulos de tareas</span><span class="sxs-lookup"><span data-stu-id="ca5a8-262">Task module samples</span></span>

* [<span data-ttu-id="ca5a8-263">EjemploNode.js/TypeScript</span><span class="sxs-lookup"><span data-stu-id="ca5a8-263">Node.js/TypeScript sample</span></span>](https://github.com/OfficeDev/microsoft-teams-sample-task-module-nodejs)
* [<span data-ttu-id="ca5a8-264">Ejemplo/.NET de C#</span><span class="sxs-lookup"><span data-stu-id="ca5a8-264">C#/.NET sample</span></span>](https://github.com/OfficeDev/microsoft-teams-sample-task-module-csharp)
